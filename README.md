# dotfiles

Personal Linux dotfiles for Hyprland and Zsh.

Included:

- `.config/hypr/`
- `.zshrc`
- `.p10k.zsh`

Excluded by default:

- `.gitconfig` because it contains personal identity details
- app data, caches, databases, and secrets
